import QuantityCounter from '../../UI/Counter/QuantityCounter';
import classes from './MealItem.module.css';

const MealItem = ({ meal }) => {
  return <div className={classes.MealItem}>Meal</div>;
};

export default MealItem;
